# Copy to keystore.properties (gitignored) after running scripts/android-setup-signing.sh
# or keytool manually. Never commit keystore.properties or *.keystore.

storeFile=wisedish-release.keystore
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=wisedish
keyPassword=YOUR_KEY_PASSWORD
