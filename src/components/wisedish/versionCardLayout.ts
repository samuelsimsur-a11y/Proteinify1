/**
 * Card layout for recipe version rows.
 * Set to `true` to restore the previous stacked layout (slots + “How to apply” in the header,
 * ingredients/instructions always visible when expanded). If you ask to “reverse” the card layout,
 * flip this flag to `true`.
 */
export const VERSION_CARD_LEGACY_LAYOUT = false;
